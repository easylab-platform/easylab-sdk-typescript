// @generated by protoc-gen-connect-es v1.6.1 with parameter "target=ts"
// @generated from file worker/v1/worker.proto (package worker.v1, syntax proto3)
/* eslint-disable */
// @ts-nocheck

// easyworker: the sandbox worker contract (Connect RPC).
//
// Replaces the legacy worker-go WebSocket JSON-RPC surface (/ws + /ws/job SSE)
// with the platform's standard Connect stack — same RPC semantics as the rest
// of easylab-platform (agent.v1 / easylab.v1), dual-stack h1+h2c, codegen via
// buf (buf.gen.worker.yaml mirrors buf.gen.easylab.yaml).
//
// Commands run through the BUILTIN shell (mvdan.cc/sh/v3 interp) on every
// platform. There is NO passthrough to a host shell and NO fallback: the
// builtin interpreter is the only executor, so bash semantics are identical
// on linux/windows/macos (including scratch/distroless images, where no shell
// exists at all). Windows batch wrappers (.cmd/.bat) are transparently
// dispatched via cmd /c inside the interp ExecHandler; kills use Job Objects
// (fallback taskkill /T).
//
// The worker is a PURE EXECUTOR: it knows nothing about repos/revs. Workspace
// sync and rev coherence are owned by easylab, which keeps the
// worker<->repo/branch mapping plus last_synced_rev in its own persistent
// metadata and detects worker restarts via Info.boot_id. (CI mode needs no
// gate either: checkout happens inside the job, like actions/checkout.)
//
// Job output history persists to sqlite (WORKER_DB; emptyDir in-cluster, so
// everything is ephemeral to the pod) with a retention window (default 24h).
//
// Server: easyworker binary (also the binary injected into cluster sandboxes;
// WORKER_PORT pins the listen port). Client: ext-ops (ops-extension).

import { ExecuteRequest, ExecuteResponse, FileListRequest, FileListResponse, FileReadRequest, FileReadResponse, FileWriteRequest, FileWriteResponse, InfoRequest, InfoResponse, JobKillRequest, JobKillResponse, JobOutputRequest, JobOutputResponse, JobStdinRequest, JobStdinResponse, JobWaitRequest, JobWaitResponse, ListJobsRequest, ListJobsResponse, WatchJobRequest, WatchJobResponse } from "./worker_pb.js";
import { MethodKind } from "@bufbuild/protobuf";

/**
 * @generated from service worker.v1.WorkerService
 */
export const WorkerService = {
  typeName: "worker.v1.WorkerService",
  methods: {
    /**
     * @generated from rpc worker.v1.WorkerService.Info
     */
    info: {
      name: "Info",
      I: InfoRequest,
      O: InfoResponse,
      kind: MethodKind.Unary,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.Execute
     */
    execute: {
      name: "Execute",
      I: ExecuteRequest,
      O: ExecuteResponse,
      kind: MethodKind.Unary,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.ListJobs
     */
    listJobs: {
      name: "ListJobs",
      I: ListJobsRequest,
      O: ListJobsResponse,
      kind: MethodKind.Unary,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.WatchJob
     */
    watchJob: {
      name: "WatchJob",
      I: WatchJobRequest,
      O: WatchJobResponse,
      kind: MethodKind.ServerStreaming,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.JobOutput
     */
    jobOutput: {
      name: "JobOutput",
      I: JobOutputRequest,
      O: JobOutputResponse,
      kind: MethodKind.Unary,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.JobWait
     */
    jobWait: {
      name: "JobWait",
      I: JobWaitRequest,
      O: JobWaitResponse,
      kind: MethodKind.Unary,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.JobStdin
     */
    jobStdin: {
      name: "JobStdin",
      I: JobStdinRequest,
      O: JobStdinResponse,
      kind: MethodKind.Unary,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.JobKill
     */
    jobKill: {
      name: "JobKill",
      I: JobKillRequest,
      O: JobKillResponse,
      kind: MethodKind.Unary,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.FileRead
     */
    fileRead: {
      name: "FileRead",
      I: FileReadRequest,
      O: FileReadResponse,
      kind: MethodKind.Unary,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.FileWrite
     */
    fileWrite: {
      name: "FileWrite",
      I: FileWriteRequest,
      O: FileWriteResponse,
      kind: MethodKind.Unary,
    },
    /**
     * @generated from rpc worker.v1.WorkerService.FileList
     */
    fileList: {
      name: "FileList",
      I: FileListRequest,
      O: FileListResponse,
      kind: MethodKind.Unary,
    },
  }
} as const;

